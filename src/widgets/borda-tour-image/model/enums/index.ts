/** Visual presentation of the tooltip image. */
export enum BordaTourImageVariant {
	/** Inset within the tooltip padding, with its own rounded corners. */
	DEFAULT = 'default',
	/** Stretched to the tooltip edges; only the top corners are rounded. */
	BLEED = 'bleed'
}
